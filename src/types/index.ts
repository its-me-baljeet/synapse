export type PDFSummaryType = {
    userId: string;
    fileUrl: string;
    summary: string;
    title: string;
    fileName: string;
}
export type StorePDFSummaryType = {
    fileUrl: string;
    summary: string;
    title: string;
    fileName: string;
}